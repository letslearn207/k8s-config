apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend1

spec:
  replicas: 2

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
      - name: frontend
        image: akshu20791/frontend:v1.0.9

        ports:
        - containerPort: 3000
